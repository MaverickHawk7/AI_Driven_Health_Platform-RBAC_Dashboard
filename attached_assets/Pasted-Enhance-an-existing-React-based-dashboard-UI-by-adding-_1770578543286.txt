Enhance an existing React-based dashboard UI by adding and wiring the following Field Worker frontend pages and flows.
Assume all data is mocked locally in the frontend. Do not implement any backend or API calls.

Constraints

Frontend only (React)

No backend, no database, no authentication logic

Use mock data and local state

Do NOT redesign existing UI components

Extend existing dashboard routes and pages

Login & Routing (Mocked)

Assume user is already authenticated as a Field Worker

Hardcode user role in frontend state

Route user directly to Field Worker Welcome Page after login

Field Worker Welcome Page

Create or extend a page that displays:

A welcome message

Two primary actions:

Register Patient

Review Patient

Register Patient Flow
Patient Registration Page

Use existing patient registration + questionnaire form

On form submit:

Store patient + questionnaire data in local state

Show a status screen/message:
“Analysis in progress…”

After a short delay (e.g., 2 seconds using setTimeout):

Update status to:
“Analysis complete”

Show a View Results button

AI Results Page (Mocked)

Navigate to a new page when View Results is clicked

Display static mock AI insights:

Risk level (Low / Medium / High)

Flagged developmental domains

Recommendation summary

Label clearly:
“AI-assisted insights (mock data, non-diagnostic)”

Review Patient Flow
Patients List Page

Display a list of previously registered patients from local state

Each patient entry shows:

Name

Last assessment date

Patient Detail Page

Selecting a patient shows:

Past questionnaire submissions

Past mock AI results

Submission timestamps

State Management

Use React state or Context API to store:

Patients

Assessments

Mock AI results

No persistence required (refresh can reset state)

Navigation

Add navigation links/buttons:

Welcome Page

Register Patient

Review Patient

Use React Router for page transitions

Out of Scope (Do NOT Build)

No backend APIs

No real authentication

No database

No AI/ML logic

No admin or supervisor views

Goal

Deliver a fully navigable frontend demo that shows:

Field Worker flow

Patient registration

Questionnaire submission

AI analysis simulation

Result viewing

Patient review

The emphasis is on user flow clarity, page transitions, and demo readiness.